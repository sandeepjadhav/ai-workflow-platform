{
    "name": "ai-workflow-platform",
    "private": true,
    "scripts": {
        "dev": "turbo run dev",
        "build": "turbo run build",
        "lint": "turbo run lint"
    },
    "devDependencies": {
        "turbo": "^2.0.0"
    },
    "dependencies": {
        "pino": "^10.3.1",
        "pino-pretty": "^13.1.3"
    }
}